import 'package:flutter/material.dart';

class GarantiasPage extends StatelessWidget {
  const GarantiasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFFFAF9F6), // fundo creme
      child: const Center(
        child: Text(
          "Tela de Garantias (em construção)",
          style: TextStyle(fontSize: 16, color: Color.fromARGB(57, 196, 142, 192)),
        ),
      ),
    );
  }
}
