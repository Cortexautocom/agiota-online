import 'package:flutter/material.dart';

class ArquivadosPage extends StatelessWidget {
  const ArquivadosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFFFAF9F6), // fundo creme
      child: const Center(
        child: Text(
          "Tela de Arquivados (em construção)",
          style: TextStyle(fontSize: 16, color: Colors.black54),
        ),
      ),
    );
  }
}
