name: clientes_web
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.6
  mask_text_input_formatter: ^2.8.0
  intl: ^0.20.2
  flutter_multi_formatter: ^2.12.0
  uuid: ^4.4.0
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.1.0  # ADICIONADO
  flutter_localizations:
    sdk: flutter
  excel: ^2.0.5
  path_provider: ^2.1.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo_agiomestre.png
    - assets/logo_empresa.png